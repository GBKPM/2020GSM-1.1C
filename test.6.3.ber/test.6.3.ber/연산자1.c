#include <stdio.h>
int main() {
	//3. 두 정수 입력 평균 계산 출력(변수2개 소수점 둘째자리까지)
	/*int a, b;
	printf("두 정수 입력: ");
	scanf_s("%d %d", &a, &b);
	printf("평균은 %.2f입니다.", (a + b) / 2.0);*/

	//4. 정수 입력 초를 시:분:초로 출력(모두 두자리 정수)
	/*int t = 0;
	int s, e, c;
	printf("초를 입력하세요: ");
	scanf_s("%d", &t);
	s = t / 3600;
	e = (t % 3600) / 60;
	c = t % 60;
	printf("%d초는 %02d시간 %02d분 %02d초입니다.\n", t, s, e, c);*/

	//5. 두 변수 값 교환 프로그램
	/*int a, b, temp=0;
	printf("두 정수 입력>> ");
	scanf_s("%d %d", &a, &b);
	printf("변환전 = a: %d, b: %d\n", a, b);
	temp = b;
	b = a;
	a = temp;
	printf("변환후 = a: %d, b: %d", a, b);*/

	return 0;
}