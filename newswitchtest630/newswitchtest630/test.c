#include <stdio.h>
#include <string.h>
int main() {
	/*int len;
	char a[10] = "gsm";
	len = strlen(a);
	for (int i = len-1; i >= 0; i--)
		printf("%c", a[i]);
	*/

	/*char a[10] = "CLanguage";
	int len, i, cnt = 0;
	len = strlen(a);
	for (i = 0; i < len; i++) {
		if (a[i] == 'a')
			cnt++;
	}
	printf("%d", cnt);*/

	/*char a[3][10] = { "apple", "banana", "carrot" };
	for (int i = 0; i < 3; i++) {
		printf("%s\n", a[i]);
	}*/
	return 0;
}