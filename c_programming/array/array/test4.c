#include <stdio.h>
int main() {
	//금요일. 난수 50개를 발생하여 실행결과와 같이 문자 막대 그래프를 작성해 보자.
	//이때 난수는 0, 1, 2값이 발생하도록 하여, 난수 값에 따라 문자배열에 특정문자를 저장한다.
	char star[50] = ""; //난수0일때 * 문자 추가
	char percent[50] = ""; //난수0일때 % 문자 추가
	char sharp[50] = ""; //난수0일때 # 문자 추가
	int count = 0;
	//난수는 rand()를 사용하여 3으로 나눈 나머지를 이용한다.
	return 0;
}